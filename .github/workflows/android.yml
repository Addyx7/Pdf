name: Android APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build APK (Docker – NO PROMPT)
        run: |
          docker run --rm \
            -e BUILDOZER_ALLOW_ROOT=1 \
            -e BUILDOZER_DISABLE_ROOT_WARNING=1 \
            -e HOME=/workspace \
            -v "$PWD":/workspace \
            -w /workspace \
            kivy/buildozer:latest \
            buildozer android debug --allow-root

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: bin/*.apk
